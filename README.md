# 📝 Projet ToDo List (Architecture MVC)

Ce projet est une application simple de gestion de tâches pour apprendre la Programmation Orientée Objet (POO) en Python.

## 📂 Structure des fichiers

*   **`task.py`** : (Modèle) Définit ce qu'est une tâche.
*   **`todo_controller.py`** : (Contrôleur) Gère la liste et les actions (ajouter, afficher).
*   **`main.py`** : (Vue/Main) Le fichier principal pour lancer le programme.

## ⚙️ Installation et Configuration

Pour travailler sur ce projet, il faut utiliser un environnement virtuel.

1.  **Créer l'environnement virtuel (une seule fois) :**
    ```bash
    python -m venv venv
    ```

2.  **Activer l'environnement virtuel (à chaque fois) :**
    *   *Windows :* `venv\Scripts\activate`
    *   *Mac/Linux :* `source venv/bin/activate`

## 🚀 Lancement

Une fois le venv activé, lance le fichier principal :

```bash
python main.py